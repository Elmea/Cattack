name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: makeWindows
      run: make CXX=x86_64-w64-mingw32-g++ BUILD_TYPE=release PACKAGE_ASSETS=true
